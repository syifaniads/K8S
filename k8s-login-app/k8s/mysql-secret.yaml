apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "REPLACE_ME"
  MYSQL_DATABASE: "loginapp"
  MYSQL_USER: "loginuser"
  MYSQL_PASSWORD: "REPLACE_ME"
  SESSION_SECRET: "REPLACE_WITH_A_LONG_RANDOM_VALUE"

# Portfolio / lab template only.
# Do not commit real credentials. For a real environment, inject secrets
# from CI/CD or an external secret-management solution.
